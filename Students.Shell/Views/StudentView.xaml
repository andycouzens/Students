<UserControl x:Class="Students.Shell.StudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:studentdata="clr-namespace:Students.Data;assembly=Students.Data"
             xmlns:local="clr-namespace:Students.Shell">
    <UserControl.DataContext>
        <local:StudentViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <DataTemplate  DataType="{x:Type studentdata:Student}">
            <TextBox x:Name="FirstName" Text="{Binding FirstName}"/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <DataGrid ItemsSource="{Binding Students}" AutoGenerateColumns="False" RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="FirstName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Path=FirstName}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <ListView Grid.Row="1" ItemsSource="{Binding Students}"/>
        
    </Grid>
</UserControl>
