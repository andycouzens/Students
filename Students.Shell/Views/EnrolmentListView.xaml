<UserControl x:Class="Students.Shell.EnrolmentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:studentdata="clr-namespace:Students.Data;assembly=Students.Data"
             xmlns:local="clr-namespace:Students.Shell"
             local:MvvmBehaviours.LoadedMethodName="LoadEnrolments">
    <UserControl.DataContext>
        <local:EnrolmentListViewModel/>
    </UserControl.DataContext>
    <Grid>
        <DataGrid ItemsSource="{Binding Enrolments}" />
    </Grid>
</UserControl>
